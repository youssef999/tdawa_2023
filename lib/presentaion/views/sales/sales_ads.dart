

 import 'package:flutter/material.dart';

class SalesAds extends StatelessWidget {
   const SalesAds({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
